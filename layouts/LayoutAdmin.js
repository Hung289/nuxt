import React from "react";

const LayoutAdmin = ({ children }) => {
    return (
        <div className="layout2">
            <h1>Admin</h1>
            {children}
        </div>
    );
};

export default LayoutAdmin;
